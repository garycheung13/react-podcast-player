import React from 'react';
import PropTypes from 'prop-types';

const ChannelListEntry = props => {
    return (
        <div>

        </div>
    );
};

ChannelListEntry.propTypes = {

};

export default ChannelListEntry;